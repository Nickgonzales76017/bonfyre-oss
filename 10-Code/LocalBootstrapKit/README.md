# Local Bootstrap Kit

One-command setup for the local transcription workflow on a fresh macOS machine.

## Run

```bash
./bootstrap.sh
```

Checks for and installs: Homebrew, ffmpeg, Python 3.10+, Whisper CLI, creates standard directory structure.
