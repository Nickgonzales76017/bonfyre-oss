# Bonfyre Distribute

Tiny native distribution utility for live offer lookup and distribution snapshot inspection.

## What It Does

- `offers` reads live offer catalog
- `snapshot` reads distribution pipeline status
- `message` generates a lightweight outreach message from a live offer

## Build

```bash
cd 10-Code/BonfyreDistribute
make
```
